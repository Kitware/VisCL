include("${CMAKE_CURRENT_LIST_DIR}/viscl-config-targets.cmake")

# include the vistk macros
include(${CMAKE_CURRENT_LIST_DIR}/viscl-macros.cmake)

# The build settings file.
SET(viscl_BUILD_SETTINGS_FILE "@viscl_BUILD_SETTINGS_FILE@")

# The viscl include directories.
SET(viscl_INCLUDE_DIR "@viscl_INCLUDE_DIR_CONFIG@" "@OPENCL_INCLUDE_DIR@")

# The viscl library directory.
SET(viscl_LIBRARY_DIR "@viscl_BINARY_DIR@")

# The viscl CMake support directory.
SET(viscl_CMAKE_DIR "@viscl_SOURCE_DIR@")

SET(viscl_VXL_DIR "@VXL_DIR@")

SET(viscl_Boost_INCLUDE_DIR "@Boost_INCLUDE_DIR@")
